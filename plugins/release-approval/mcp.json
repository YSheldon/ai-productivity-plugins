{
  "mcpServers": {
    "release-approval": {
      "command": "py",
      "args": [
        "-3",
        "${PLUGIN_ROOT}/src/release_approval_mcp.py"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
