# 记忆管家 nest

Home for the memory-manager bot itself (not fleet business archives).

- `remotes/registry.md` — catalog of cloud backup endpoints
- `self/` — 记忆管家 self MEMORY (public-safe)
- `chats/` — optional condensed 1:1 notes (no secrets)

Fleet disaster-recovery lives on private remotes (see remotes registry), locally under `/workspace/memory-manager`.
