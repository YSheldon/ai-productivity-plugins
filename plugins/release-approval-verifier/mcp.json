{
  "mcpServers": {
    "release-approval-verifier": {
      "command": "py",
      "args": [
        "-3",
        "${PLUGIN_ROOT}/src/release_approval_verifier_mcp.py"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
