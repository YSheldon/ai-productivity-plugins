{
  "mcpServers": {
    "submission-gate": {
      "command": "py",
      "args": [
        "-3",
        "${PLUGIN_ROOT}/src/submission_gate_mcp.py"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
