{
  "mcpServers": {
    "imap-smtp-mail": {
      "command": "cmd.exe",
      "args": [
        "/d",
        "/c",
        "${PLUGIN_ROOT}/scripts/launch_cursor_mcp.cmd"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
